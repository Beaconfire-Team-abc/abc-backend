server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Beaconfire-Team-abc/cloud-config.git
          clone-on-start: true
          skip-ssl-validation: true