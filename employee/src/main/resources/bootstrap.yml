spring:
  application:
    name: employee
  cloud:
    config:
      name: employee
      label: abc
      uri: "http://localhost:8888"
  profiles:
    active: development
  mvc:
    servlet:
      path: /employee